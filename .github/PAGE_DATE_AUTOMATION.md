# Automatic page update dates

The `Update page dates` workflow runs after pushes to `main`.
It updates the footer date only for affected pages and initializes any page that still has the legacy footer.
Dates are generated in the `Asia/Tokyo` time zone.
